/* variable "tags_hourly" {
    type        = map(string)
    description = "(Optional) Following the costs of S3 tagging your bucket"
    default = { reports = "hourly"}
}

variable "tags_daily" {
    type        = map(string)
    description = "(Optional) Following the costs of S3 tagging your bucket"
    default = { reports = "daily"}
}

variable "tags_weekly" {
    type        = map(string)
    description = "(Optional) Following the costs of S3 tagging your bucket"
    default = { reports = "weekly"}
} */